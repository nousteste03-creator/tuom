import { Spacing } from "./microcards";

export const Layout = {
  screenPadding: {
    paddingHorizontal: Spacing.lg,
    paddingTop: Spacing.lg,
  },

  card: {
    padding: Spacing.md,
    borderRadius: 14,
  },

  row: {
    flexDirection: "row",
    alignItems: "center",
  },
};
